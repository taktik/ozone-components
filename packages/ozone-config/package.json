{
  "name": "ozone-config",
  "version": "0.0.1",
  "description": "Ozone config loader",
  "repository": {
    "type": "git",
    "url": "https://github.com/taktik/ozone-config"
  },
  "main": "dist/ozone-config.js",
  "types": "dist/ozone-config.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "tsc && git diff --exit-code"
  },
  "author": "hj@taktik",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.4.1"
  },
  "dependencies": {
    "ozone-api-request": "^0.0.1",
    "taktik-polymer-typescript": "^0.0.1"
  }
}
